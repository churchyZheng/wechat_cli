#!/bin/bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"
PYTHON_BIN="${WECHAT_PYTHON:-/Users/churchy/aidierenv/bin/python3}"

exec "$PYTHON_BIN" "$ROOT_DIR/scripts/wechat_cli.py" "$@"
